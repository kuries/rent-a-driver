<!-- Nav Bar -->
<nav class=" navbar navbar-custom navbar-expand-lg fixed-top navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Rent-A-Driver</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="/driver">Driver</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/dealer">Dealer</a>
                </li>
            </ul>
            <% if (check == true) { %>
            <ul class="d-flex navbar-nav">
                <li class="nav-item">
                    <a class="btn btn-outline-light" href="/logout">Logout</a>
                </li>
            </ul>
            <% } %>
        </div>
    </div>
</nav>


<% if (typeof flashMessage != 'undefined'  && flashMessage.length > 0) {%>
    <div class="row">
        <div class="col-12 text-center d-flex justify-content-center" style="padding-top: 60px;">
            <div id="myalert" class="alert alert-warning alert-dismissible fade show" role="alert">
                <%= flashMessage[0] %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div> 
        </div>
    </div>
<% } %>